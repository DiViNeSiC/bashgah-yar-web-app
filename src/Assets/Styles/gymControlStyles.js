import { makeStyles } from "@material-ui/core";

export const editGymFormStyles = makeStyles(() => ({

}))

export const multiGymViewStyles = makeStyles(() => ({

}))

export const oneGymViewStyles = makeStyles(() => ({

}))

export const gymStaffViewStyles = makeStyles(() => ({

}))