import { makeStyles } from "@material-ui/core"

export const registerFormStyles = makeStyles(() => ({

}))

export const avatarInputStyles = makeStyles(() => ({

}))

export const gymPictureInputStyles = makeStyles(() => ({

}))

export const activeAccountLayoutStyles = makeStyles(() => ({

}))