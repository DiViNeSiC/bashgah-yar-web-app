import { makeStyles } from "@material-ui/core"

export const editProfileFormStyles = makeStyles(() => ({

}))

export const changePassFormStyles = makeStyles(() => ({

}))

export const singleUserViewStyles = makeStyles(() => ({

}))

export const multiUserViewStyles = makeStyles(() => ({

}))